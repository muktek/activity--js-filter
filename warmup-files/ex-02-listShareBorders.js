
/**
 * Use `.filter()` to filter  countries in Europe that share more than 5 borders
 *
 *   The data is already stored as an array in a var called `countriesDataList`
 *
 *   The output of the filter method should go into an array called
 *   `shareManyBordersList`
**/


// -----  TEST Assertions ------
console.assert(typeof shareManyBordersList !== 'undefined')
console.assert(Array.isArray(shareManyBordersList)=== true)
console.assert(shareManyBordersList.length === 9)
