/**
 *  Use `.filter()` to iterate over the DOM elements from arrayOfElements
 *   and filter for elements that have a class value of 'incomplete'
 *
 *    -- next then put the number of incomplete tasks in the
 *       <span class="incomplete-task-count"></span> element
**/


let arrayOfElements = [...document.querySelectorAll('li')]
